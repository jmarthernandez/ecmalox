// import { Lox } from "./index";

// describe("Lox", () => {
//   describe("prints usage if args.length > 1", () => {
//     const lox = new Lox();
//     lox.main(["path", "second path"]);
//     // figure out how to mock console
//     // expect(main.runPrompt).toHaveBeenCalled();
// });

// describe("calls runFile if args === 1", () => {
//     const args = ['path'];
//     const lox = new Lox();
//     lox.main(args);
//     // expect(lox.runFile).toHaveBeenCalledWith(args);
// });

// describe("calls runPrompt if args === 0", () => {
//     const lox = new Lox();
//     lox.main([]);
//     // expect(lox.runPrompt).toHaveBeenCalledWith([]);
//   });
// });
